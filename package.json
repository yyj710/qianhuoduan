{
  "name": "chuangyin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npx vite build",
    "copy:frontend": "node -e \"const fs=require('fs');const p=require('path').join('backend','dist','public');fs.mkdirSync(p,{recursive:true});fs.cpSync('frontend/dist',p,{recursive:true});console.log('Frontend copied')\"",
    "build:backend": "cd backend && npm install && npx prisma generate && npx prisma db push && npm run build",
    "build": "npm run build:frontend && npm run copy:frontend && npm run build:backend",
    "start": "cd backend && npm start"
  }
}
