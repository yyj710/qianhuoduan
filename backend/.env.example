# 本地开发用的配置
# 先按照下面的步骤创建 Neon 数据库，拿到连接字符串后替换下面这行
# 步骤：https://console.neon.tech → Create project → 复制 Connection string
DATABASE_URL="postgresql://用户名:密码@主机:5432/数据库名?sslmode=require"
JWT_SECRET="换成你自己的随机长字符串"
NODE_ENV=development
PORT=3000
